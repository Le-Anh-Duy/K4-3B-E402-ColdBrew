# Public backend origin only. Empty uses the existing Vite /api proxy.
VITE_API_BASE_URL=
# api = real supported endpoints, explicit demo practice for unsupported flows.
# demo = fully offline demo, for UI tests only.
VITE_DATA_MODE=api
