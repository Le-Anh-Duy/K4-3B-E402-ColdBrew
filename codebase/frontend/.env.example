# Public backend origin only. Empty uses the existing Vite /api proxy.
VITE_API_BASE_URL=
# api = enable real backend health checks; learning/auth remain explicit demo
# because the current backend only has /api/health and /api/llm-check.
# demo = offline UI mode, disabling the automatic health check.
VITE_DATA_MODE=api
