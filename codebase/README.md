- `backend/` — FastAPI + Gemini qua SDK OpenAI (`base_url` endpoint OpenAI-compatible của Google).
- `frontend/` — React + Vite.

Trạng thái: **skeleton** — chưa có logic sản phẩm (quiz, graph, chẩn đoán, lộ trình). Xem `spec.md` §4.
