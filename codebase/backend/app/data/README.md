# Dữ liệu mock của backend

**Ba file JSON ở đây được SINH TỰ ĐỘNG** từ `mockup/data.js`:

```
node scripts/export_graph.js
```

Sửa tay là mất khi chạy lại generator.

**Đây là dữ liệu mock để kiểm thử**, không phải data pack của khoá: cây tri thức do nhóm dựng
tay, mỗi node mang **mã đoạn** `[T01-xxx]` trỏ về transcript gốc (transcript **không** nằm trong
repo). Xem [`DATA_NOTICE.md`](../../../../DATA_NOTICE.md).
